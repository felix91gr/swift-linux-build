## Autogenerated from /home/felix/Desktop/NativeSetup/swift-linuxSetup/native_install/llvm/projects/compiler-rt/test/asan/Unit/lit.site.cfg.in
## Do not edit!

import os

# Load common config for all compiler-rt unit tests.
lit_config.load_config(config, "/home/felix/Desktop/NativeSetup/swift-linuxSetup/native_install/build/2pass/llvm-linux-x86_64/tools/clang/runtime/compiler-rt-bins/unittests/lit.common.unit.configured")

def push_ld_library_path(config, new_path):
  new_ld_library_path = os.path.pathsep.join(
    (new_path, config.environment.get('LD_LIBRARY_PATH', '')))
  config.environment['LD_LIBRARY_PATH'] = new_ld_library_path

# Setup config name.
config.name = 'AddressSanitizer-Unit'

# Setup test source and exec root. For unit tests, we define
# it as build directory with ASan unit tests.
# FIXME: De-hardcode this path.
if True:
  test_dir = "dynamic"
else:
  test_dir = "default"
config.test_exec_root = os.path.join("/home/felix/Desktop/NativeSetup/swift-linuxSetup/native_install/build/2pass/llvm-linux-x86_64/tools/clang/runtime/compiler-rt-bins",
                                     "lib", "asan", "tests", test_dir)

config.test_source_root = config.test_exec_root

# Set LD_LIBRARY_PATH to pick dynamic runtime up properly.
push_ld_library_path(config, config.compiler_rt_libdir)
